'use strict';

import React, { PureComponent, PropTypes } from 'react';

import './index.scss';

export default class Tagging extends PureComponent {
  static defaultProps = {

  };

  static propTypes = {

  };

  constructor(props) {
      super(props);
  }

  render() {
      return (
        <div>Hello World</div>
      );
  }
}
